{
  "buildCommand": "npm install && npm run build",
  "ignoreCommand": "echo Ignoring cached node_modules",
  "cleanUrls": true,
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": ""
        }
      ]
    }
  ]
}

